# This is a Databricks asset bundle definition for gam_lakehouse.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: gam_lakehouse
  uuid: c2e84e41-0999-495a-baa6-38dc07e3e1f0

# artifacts:
#   python_artifact:
#     type: whl
#     build: python setup.py bdist_wheel

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  # catalog:
  #   description: The catalog to use
  # schema:
  #   description: The schema to use
    bronze_catalog:
      description: catalog for bronze layer
    silver_catalog:
      description: catalog for silver layer
    gold_catalog:
      description: catalog for gold silver
    schema:
      description: schema
targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-fa6d56c6-2a1a.cloud.databricks.com
      root_path: /Workspace/Users/leoxin95@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      # catalog: workspace
      # schema: ${workspace.current_user.short_name}
      bronze_catalog: dev_bronze
      silver_catalog: dev_silver
      gold_catalog: dev_gold
      schema: gam

  preprod:
    mode: production
    workspace:
      host: https://dbc-fa6d56c6-2a1a.cloud.databricks.com
      root_path: /Workspace/Users/leoxin95@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      bronze_catalog: preprod_bronze
      silver_catalog: preprod_silver
      gold_catalog: preprod_gold
      schema: gam

  prod:
    mode: production
    workspace:
      host: https://dbc-fa6d56c6-2a1a.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/leoxin95@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/leoxin95@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      # catalog: workspace
      # schema: prod
      bronze_catalog: prod_bronze
      silver_catalog: prod_silver
      gold_catalog: prod_gold
      schema: gam
    permissions:
      - user_name: leoxin95@gmail.com
        level: CAN_MANAGE
